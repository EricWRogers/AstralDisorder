using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Diff_Level_Gen : MonoBehaviour
{
    // Start is called before the first frame update 
    //set up difficulty levels so the work where easy is 3 speed normal is 6 hard is 7 extreme is 9
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
